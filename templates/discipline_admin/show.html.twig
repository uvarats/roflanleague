{% extends 'base.html.twig' %}

{% block title %}Discipline{% endblock %}

{% block body %}
    <h1>Discipline</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discipline.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ discipline.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ discipline.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_discipline_admin_index') }}">back to list</a>

    <a href="{{ path('app_discipline_admin_edit', {'id': discipline.id}) }}">edit</a>

    {{ include('discipline_admin/_delete_form.html.twig') }}
{% endblock %}
