{% extends 'base.html.twig' %}

{% block title %}Новая дисциплина{% endblock %}

{% block body %}

    {{ include('header.html.twig') }}

    <div class="container">
        <div class="d-flex flex-column justify-content-center text-center">
            <h1>Создание новой дисциплины</h1>
            <a class="btn btn-outline-primary" href="{{ path('app_admin_discipline_index') }}">К листингу</a>
        </div>
        <hr>

        <div>
            {{ include('admin/discipline/_form.html.twig') }}
        </div>

    </div>

    {{ include('footer.html.twig') }}

{% endblock %}
