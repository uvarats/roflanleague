{% extends 'base.html.twig' %}

{% block title %}Рофлолига - Админ-панель!{% endblock %}

{% block body %}

    {{ include('header.html.twig') }}
    <div class="container">
        <div class="container-fluid py-4">
            <div class="d-flex justify-content-center">
                <span class="h3">Привет, {{ app.user.username }}. Это админ-панель!</span>
            </div>
            <div class="list-group mt-3">
                <a href="{{ path('app_admin_tourneys') }}" class="list-group-item list-group-item-action">Управление турнирами</a>
                <a href="{{ path('app_admin_users') }}" class="list-group-item list-group-item-action">Управление пользователями</a>
                <a href="{{ path('app_admin_badges') }}" class="list-group-item list-group-item-action">Управление значками</a>
                <a href="{{ path('app_admin_discipline_index') }}" class="list-group-item list-group-item-action">Управление дисциплинами</a>
            </div>
        </div>
    </div>
    {{ include('footer.html.twig') }}

{% endblock %}
